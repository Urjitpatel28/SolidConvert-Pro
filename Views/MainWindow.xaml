<Window x:Class="SolidConvert_Pro.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:SolidConvert_Pro.Converters"
        xmlns:models="clr-namespace:SolidConvert_Pro.Models"
        mc:Ignorable="d"
        Title="SolidConvert" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        AllowDrop="True"
        Background="#F5F5F5">
    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        
        <!-- Styles -->
        <Style TargetType="Button">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Label">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="SolidConvert" 
                   FontSize="28" FontWeight="Bold" 
                   Foreground="#2196F3" 
                   Margin="0,0,0,20"/>

        <!-- Export Format Selection -->
        <GroupBox Grid.Row="1" Header="Export Format" Margin="0,0,0,15" Padding="15">
            <StackPanel Orientation="Horizontal">
                <CheckBox Content="Export to PDF" 
                         IsChecked="{Binding ExportToPdf}" 
                         IsEnabled="{Binding IsConverting, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"/>
                <CheckBox Content="Export to DWG" 
                         IsChecked="{Binding ExportToDwg}" 
                         IsEnabled="{Binding IsConverting, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=invert}"/>
            </StackPanel>
        </GroupBox>

        <!-- File Selection Section -->
        <GroupBox Grid.Row="2" Header="Selected Files" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- File List -->
                <ListBox Grid.Row="1" 
                        ItemsSource="{Binding SelectedFiles}"
                        SelectionMode="Extended"
                        Margin="0,10,0,10"
                        BorderThickness="1"
                        BorderBrush="#CCCCCC"
                        x:Name="FilesListBox">
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:FileItem}">
                            <Grid Margin="5">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" FontSize="14"/>
                                    <TextBlock Text="{Binding FilePath}" FontSize="11" Foreground="Gray" TextTrimming="CharacterEllipsis"/>
                                    <TextBlock Text="{Binding ErrorMessage}" FontSize="11" Foreground="Red" 
                                              Visibility="{Binding ErrorMessage, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                                <Ellipse Grid.Column="1" Width="12" Height="12" Margin="10,0,0,0"
                                        Fill="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                                        VerticalAlignment="Center"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <!-- File Buttons -->
                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Add Files" Command="{Binding AddFilesCommand}"/>
                    <Button Content="Remove Selected" Command="{Binding RemoveSelectedFilesCommand}" 
                           CommandParameter="{Binding SelectedItems, ElementName=FilesListBox}"/>
                    <Button Content="Clear List" Command="{Binding ClearListCommand}"/>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Output Folder Section -->
        <GroupBox Grid.Row="3" Header="Output Folder" Margin="0,0,0,15" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox Grid.Column="0" Text="{Binding OutputFolder, UpdateSourceTrigger=PropertyChanged}" 
                        IsReadOnly="True" Margin="0,0,10,0"/>
                <Button Grid.Column="1" Content="Browse" Command="{Binding SelectOutputFolderCommand}"/>
            </Grid>
        </GroupBox>

        <!-- Progress Section -->
        <GroupBox Grid.Row="4" Header="Progress" Margin="0,0,0,15" Padding="15">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding CurrentFile}" FontSize="12" Foreground="#2196F3" Margin="0,0,0,10"
                          Visibility="{Binding CurrentFile, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <ProgressBar Value="{Binding Progress}" Maximum="100" Height="25" Margin="0,0,0,5"/>
                <TextBlock HorizontalAlignment="Right" FontSize="12" Foreground="Gray">
                    <Run Text="{Binding FilesProcessed}"/>
                    <Run Text="/"/>
                    <Run Text="{Binding TotalFiles}"/>
                    <Run Text="("/>
                    <Run Text="{Binding Progress}"/>
                    <Run Text="%)"/>
                </TextBlock>
            </StackPanel>
        </GroupBox>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Export" Command="{Binding ExportCommand}" 
                   Background="#4CAF50" FontWeight="Bold" FontSize="16" Padding="20,10"
                   Width="150">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#45A049"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Content="Cancel" Command="{Binding CancelCommand}" 
                   Background="#F44336" 
                   Visibility="{Binding IsConverting, Converter={StaticResource BoolToVisibilityConverter}}"
                   Width="150">
                <Button.Style>
                    <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#DA190B"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>

